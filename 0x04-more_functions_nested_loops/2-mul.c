#include "main.h"

/**
 * mul - Multiply two integers
 * @x: The first number to multiply
 * @y: The second number to multiply
 * Return: Result of x multiplied by y
 */

int mul(int x, int y)
{
	return (x * y);
}
